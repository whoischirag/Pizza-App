<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="icon" href="/img/oglogo.png" type="img/png" />
    <link rel="stylesheet" href="/css/app.css" />
    <title>Pizza App</title>
</head>
<body>
    <nav class="container mx-auto flex items-center justify-between p-4">
        <div>
           <a href="/"> <img src="/img/logo.png" alt="Pizza App Logo" class="max-w-xs h-auto" /></a>
        </div>

        <div>
            <ul class="flex items-center">
                <li class="ml-6"><a href="/" class="hover:text-blue-500">Menu</a></li>
                <li class="ml-6"><a href="#" class="hover:text-blue-500">Offers</a></li>
                <% if(user){ %>
                  <li class="ml-6">
                    <form id="logout" action="/logout" method="POST">
                        <a onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                    </form>
                </li>
                
                <% } else { %>
                <li class="ml-6"><a href="/register" class="hover:text-blue-500">Register</a></li>
                <li class="ml-6"><a href="/login" class="hover:text-blue-500">Login</a></li>
                <% } %>
                <li class="ml-6">
                    <a href="/cart" class="inline-block rounded-full px-4 py-2 flex items-center">
                        <span id="cartCounter" class="text-white font-bold pr-2">
                            <%= session && session.cart ? session.cart.totalQty : 0 %>
                        </span>
                        <img src="/img/cart.png" alt="Cart" />
                    </a>
                </li>
            </ul>
        </div>
    </nav>

    <%- body %>
    <script src="/js/app.js"></script>
</body>
</html>
